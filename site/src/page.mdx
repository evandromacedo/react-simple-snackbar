import Teste from './components/Teste/Teste'

<h1 id="getting-started">Getting Started</h1>

## Installation

```??? noLines
npm install --save react-simple-snackbar
```

## Basic Usage

First, you need to wrap your application into a `SnackbarProvider`:

```jsx
// App.js
import React from 'react'
import { render } from 'react-dom'
import SnackbarProvider from 'react-simple-snackbar'
import Content from './Content'

const App = () => (
  <SnackbarProvider>
    <Content />
  </SnackbarProvider>
)

render(<App />, document.querySelector('#root'))
```

Then you can use the `useSnackbar()` hook in any child component:

```jsx
// Content.js
import React from 'react'
import { useSnackbar } from 'react-simple-snackbar'

export default function Content() {
  const [open] = useSnackbar()

  return (
    <button onClick={() => open('This is an example of a simple Snackbar.')}>
      Click me to open the Snackbar!
    </button>
  )
}
```

Run this example: <Teste />

Lorem ipsum dolor sit amet consectetur adipisicing elit. Ipsa illo, repellendus rerum eum accusamus beatae totam itaque excepturi fuga, dicta enim dolore cupiditate quae corporis consectetur, delectus molestias repudiandae deserunt.

<h1 id="documentation">Documentation</h1>
